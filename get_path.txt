argv = {"a.out", "infile", "cmd1", "cmd2", "outfile"};
cmd1 = "ls -l"
ft_split(cmd1, ' ')
	= {"ls", "-l"} = cmds1
environ = {"...", "...", "PATH...", "...", ...};
path_envp = strnstr(environ[i], "PATH");
path_envp += 5;
splited_path = ft_split(path_envp, ':');
path_cmd = ft_strjoin(splited_path, "/cmd1[0]");
if (!access(path_cmd, X_OK))
	there is!;
